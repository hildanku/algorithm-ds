/*
 * Basic Array
 *
 *
 */

const arr: number[] = [1, 2, 3, 4, 5]

arr.push(6, 7) // add value or more to last of array, expected return is [1,2,3,4,5,6]
// arr.pop()   // delete value from last of array, expected return is [1,2,3,4,5]
// arr.shift() // delete first value of array, expected return is [2,3,4,5]
// arr.unshift(0, 1, 11) // add value or more to first of array, expected return is [0,1,11,2,3,4,5]

console.log(arr)
